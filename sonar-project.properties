sonar.projectKey=homelab-terraform-localstack
sonar.projectName=Homelab Terraform (LocalStack)
sonar.sources=.
sonar.sourceEncoding=UTF-8

# Build outputs and provider caches are not source code
sonar.exclusions=**/.terraform/**,**/*.tfstate*,**/tfplan,**/.scannerwork/**
